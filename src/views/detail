<template>
  <div class="container mt-4">
    <div class="row">
      <!-- Colonna stretta (4 su 12) con immagine -->
      <div class="col-md-4">
        <div class="sticky">
          <img
            src="link-all'immagine.jpg"
            alt="Immagine"
            class="rounded-image"
          />
        </div>
      </div>

      <!-- Colonna ampia (8 su 12) con dettagli dell'evento -->
      <div class="col-md-8">
        <h1>Nome dell'Evento</h1>
        <div class="mb-3">
          <label for="evento">Nome dell'Evento</label>
          <input
            type="text"
            class="form-control"
            id="evento"
            placeholder="Inserisci il nome dell'evento"
          />
        </div>
        <div class="mb-3">
          <label for="data_ora">Data e Ora</label>
          <input
            type="text"
            class="form-control"
            id="data_ora"
            placeholder="Inserisci la data e l'ora dell'evento"
          />
        </div>
        <div class="jumbotron">
          <h3>Prezzo:</h3>
          <p>Descrizione del prezzo e ulteriori dettagli qui.</p>
        </div>
      </div>
    </div>
  </div>
</template>

<style>
.sticky {
  position: sticky;
  top: 20px;
}
.rounded-image {
  border-radius: 50%;
  max-width: 100%;
  height: auto;
}
</style>
